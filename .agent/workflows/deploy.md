---
description: Deploy to GitHub Pages
---
// turbo-all

1. Stage all changes: `git add -A`
2. Commit with descriptive message: `git commit -m "<describe changes>"`
3. Push to origin: `git push origin master`

Site will be live at https://shembree89.github.io/Cron/ within 1-2 minutes.
